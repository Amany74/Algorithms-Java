package com.company;

public class mergeSort {

  /*
    public void mergeSort (int arr []) {
        //base case for the recursion
        if (arr.length < 2)   return;
        //divide these array into half
        // paste elements in the given array into the new halves
        int middle = arr.length / 2;
        int[] left = new int[middle];

        for (int i = 0; i < middle; i++){
            left[i] = arr[i];  //(i-middle) lmn3 al tekrar
    }
        int [] right = new int [arr.length - middle ];

        for(int i = middle ; i < arr.length ; i++) {
            right[i] = arr[i];
        }
        // sort each half
        mergeSort(left);
        mergeSort(right);

        merge (left,right,arr);


    }

    // merge function to merge the two arrays left & right
    public void  merge(int [] left , int [] right , int result []) {

        int i = 0 , j = 0 , k = 0 ;

        while(i < left.length &&  j < right.length){
            if(left[i] <= right[j])
                result[k++] = left[i++];
            else
                result[k++] = right[j++];

            // tb lw 3nasr ay array mnhom 5lst

            while(i < left.length)
                result[k++] = left[i++];


            while(j < right.length)
                result[k++] = right[j++];

        }

    }



*/
















}
